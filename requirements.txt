torch>=1.10.0
torchvision>=0.11.0
numpy>=1.20.0
Pillow>=8.0.0
matplotlib>=3.3.0
scipy>=1.7.0
tqdm>=4.60.0
# 可选依赖
thop>=0.1.0          # 用于 FLOPs 统计 (缺失时脚本会自动跳过)
onnx>=1.10.0         # 仅 ONNX 导出需要
onnxruntime>=1.10.0  # 仅 ONNX 验证需要
